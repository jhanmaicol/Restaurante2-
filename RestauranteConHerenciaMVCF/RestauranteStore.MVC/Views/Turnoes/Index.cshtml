@model IEnumerable<RestauranteStore.Entities.Entities.Turno>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TurnoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TurnoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TurnoId })
        </td>
    </tr>
}

</table>
