@model IEnumerable<RestauranteStore.Entities.Entities.EstadoPedido>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstadoPedidoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstadoPedidoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstadoPedidoId })
        </td>
    </tr>
}

</table>
