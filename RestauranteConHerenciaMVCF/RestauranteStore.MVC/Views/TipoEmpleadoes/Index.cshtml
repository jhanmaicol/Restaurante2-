@model IEnumerable<RestauranteStore.Entities.Entities.TipoEmpleado>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomTipoEmp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomTipoEmp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoEmpleadoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoEmpleadoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoEmpleadoId })
        </td>
    </tr>
}

</table>
